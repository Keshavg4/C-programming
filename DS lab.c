//wap to create an array of size 10 insert 10 elements.define a function that will except the array and size
//,the function should return even , odd, zero numbers.\\

////NOTE--A function can return atmost one value only.so now the updated program is

#include <stdio.h>
void write(int arr[] , int s)
{
    for( int i = 0; i<s; i++)
    {
        printf("%d",arr[i]);
    }
}

int main()
{


}

